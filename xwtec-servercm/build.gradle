
dependencies {
    compile(
            libraries."servlet-api",
            libraries."log4j",
            libraries."slf4j-log4j12",
            libraries."slf4j-api",
            libraries."jyaml",
            libraries."jackson-databind",
    )
}